apiVersion: v1
kind: Service
metadata:
    name: "twiddit-8098"
    labels:
        app: "twiddit-8098"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twiddit-8098"