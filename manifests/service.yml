apiVersion: v1
kind: Service
metadata:
    name: "twidditcluster-b224"
    labels:
        app: "twidditcluster-b224"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twidditcluster-b224"