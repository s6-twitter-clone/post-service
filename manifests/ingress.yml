apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "twidditcluster-b224"
    labels:
        app: "twidditcluster-b224"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-twidditcluster-b224.bdc33b62f0d9413db518.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "twidditcluster-b224"
                servicePort: 80