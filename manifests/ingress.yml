apiVersion: extensions/v1beta1
kind: Ingress
metadata:
    name: "twiddit-8098"
    labels:
        app: "twiddit-8098"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
spec:
    rules:
        - host: default-twiddit-8098.5fd06ac12e634d9096be.uksouth.aksapp.io
          http:
            paths:
            - path: /
              backend:
                serviceName: "twiddit-8098"
                servicePort: 80